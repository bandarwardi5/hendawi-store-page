# مشروع متجر هنداوي (Hendawi Store)

## نظرة عامة على المشروع
متجر هنداوي هو تطبيق ويب للتجارة الإلكترونية مبني باستخدام أحدث تقنيات تطوير الواجهات الأمامية لضمان تجربة مستخدم سريعة وسلسة:
- **إطار العمل:** React 19 مع ميزات متقدمة.
- **التوجيه وإدارة الحالة:** `@tanstack/react-router` و `@tanstack/react-query`.
- **التصميم والواجهة:** Tailwind CSS و Radix UI (shadcn/ui) لواجهات عصرية ومتجاوبة.
- **إدارة النماذج:** `react-hook-form` و `zod` للتحقق من صحة المدخلات.

يقدم المتجر حالياً واجهة مستخدم ممتازة تحتوي على صفحات للتصنيفات (الإلكترونيات، الأزياء، العطور، الساعات، الكتب) وتصميم عام متجاوب، لكنه يفتقر إلى الوظائف الديناميكية الأساسية.

---

## الأجزاء الناقصة الأساسية في المتجر

لكي يصبح المتجر جاهزاً للعمل الفعلي، يحتاج إلى استكمال الأنظمة التالية:

1. **سلة المشتريات وعملية الدفع (Cart & Checkout):**
   - نظام لإضافة المنتجات، تعديل الكميات، وحساب الإجمالي.
   - صفحة لإتمام الطلب (Checkout) وإدخال بيانات الشحن.
   - الربط مع بوابات الدفع الإلكتروني (مثل Stripe أو بوابات محلية).

2. **نظام حسابات المستخدمين (Authentication & User Profiles):**
   - صفحات لتسجيل الدخول وإنشاء حساب جديد.
   - لوحة تحكم للعميل تعرض (سجل الطلبات، تتبع الشحنات، العناوين، المنتجات المفضلة).

3. **صفحة تفاصيل المنتج الديناميكية (Single Product Page):**
   - صفحة مخصصة لعرض تفاصيل كل منتج بناءً على المعرف (ID)، تشمل معرض صور، وصف مفصل، تقييمات، واختيار المتغيرات (مثل المقاس أو اللون).

4. **لوحة تحكم الإدارة (Admin Dashboard):**
   - واجهة مخصصة لمدير المتجر لإضافة/تعديل المنتجات، إدارة المخزون، ومتابعة حالات الطلبات.

5. **إدارة البيانات والواجهة الخلفية (Database & Backend):**
   - الانتقال من البيانات الثابتة (Mock Data) إلى قاعدة بيانات فعلية لتخزين المنتجات، المستخدمين، والطلبات.

6. **البحث والتصفية المتقدمة (Search & Filtering):**
   - محرك بحث للمنتجات وفلاتر متقدمة (حسب السعر، التقييم، العلامة التجارية).

7. **تعدد اللغات (i18n):**
   - دعم اللغتين العربية والإنجليزية إذا كان المتجر يستهدف جمهوراً متنوعاً.

---

## خطة دمج Firebase (الخطوة القادمة)

بما أنك أردت استخدام **Firebase**، فإن هذه خطوة ممتازة ستساعدنا في تغطية العديد من النواقص المذكورة أعلاه (تحديداً النقطتين 2 و 5). 

> [!TIP]
> سيقوم Firebase بتوفير الواجهة الخلفية كاملة للمشروع (BaaS)، مما يسرع عملية التطوير بشكل كبير دون الحاجة لبرمجة خوادم معقدة.

### كيفية استخدام Firebase لحل النواقص:
- **Firebase Authentication:** سيُستخدم لبرمجة نظام تسجيل الدخول وإنشاء الحسابات للعملاء.
- **Cloud Firestore (Database):** سيُستخدم كقاعدة بيانات لتخزين المنتجات، تصنيفاتها، محتويات سلة المشتريات، وبيانات الطلبات.
- **Firebase Storage:** سيُستخدم لرفع وتخزين صور المنتجات.
- **ترحيل البيانات (Data Migration):** تجهيز سكريبت لنقل بيانات المنتجات والتصنيفات الحالية (Demo Data) إلى Firestore.

### التعديلات المقترحة (Proposed Changes)

#### [MODIFY] `package.json`
- إضافة مكتبة `firebase` إلى التبعيات (Dependencies).

#### [NEW] `src/lib/firebase.ts`
- إنشاء ملف إعدادات Firebase باستخدام البيانات التي قدمتها:
```typescript
import { initializeApp } from "firebase/app";
import { getAuth } from "firebase/auth";
import { getFirestore } from "firebase/firestore";
import { getStorage } from "firebase/storage";

const firebaseConfig = {
  apiKey: "AIzaSyBQOuX4og4AhyNo4kRfHSUFKfo2CqUik1k",
  authDomain: "hendawi-store.firebaseapp.com",
  projectId: "hendawi-store",
  storageBucket: "hendawi-store.firebasestorage.app",
  messagingSenderId: "773201910311",
  appId: "1:773201910311:web:9ae82bdd5d8a624e71cc02",
  measurementId: "G-442KCZ5Q8V"
};

const app = initializeApp(firebaseConfig);
export const auth = getAuth(app);
export const db = getFirestore(app);
export const storage = getStorage(app);
```
